/**
 * @license
 * Copyright 2025 Google Inc.
 * SPDX-License-Identifier: Apache-2.0
 */
import type { ConnectionTransport } from '../common/ConnectionTransport.js';
import type { BrowserWorker } from './BrowserWorker.js';
import { type Browsers, type SessionGuardrails } from './utils.js';
/**
 * Whatever shapes the upgrade request beyond the endpoint and the session, kept
 * in one bag so that adding another knob doesn't grow the `create` signature.
 */
export interface WorkersWebSocketTransportOptions {
    browser?: Browsers;
    guardrails?: SessionGuardrails;
}
export declare class WorkersWebSocketTransport implements ConnectionTransport {
    ws: WebSocket;
    onmessage?: (message: string) => void;
    onclose?: () => void;
    sessionId: string | undefined;
    static create(endpoint: BrowserWorker, sessionId: string | undefined, options?: WorkersWebSocketTransportOptions): Promise<WorkersWebSocketTransport>;
    constructor(ws: WebSocket, sessionId: string | undefined);
    send(message: string): void;
    close(): void;
    toString(): string;
}
//# sourceMappingURL=WorkersWebSocketTransport.d.ts.map